server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/omincharge
    username: aman
    password: tiger
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
  instance:
    prefer-ip-address: true